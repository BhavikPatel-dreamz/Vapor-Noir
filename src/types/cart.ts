export type CartItem = {
  id: string;
  productId: string;
  variantId: string;
  slug: string;
  name: string;
  variantName: string;
  price: number;
  currency: string;
  image: string;
  quantity: number;
};
